import { Typography } from '@mui/material';

export default function Dashboard() {
  return (
    <Typography variant="h5" sx={{ mt: 2 }}>
      歡迎進入後台管理系統 👋
    </Typography>
  );
}